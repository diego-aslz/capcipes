default: &default
  host: <%= fetch(:database_host) { "localhost" } %>
  port: 5432
  adapter: postgresql
  encoding: utf8
  pool: 5

production:
  <<: *default
  database: <%= application_name %>
  username: <%= application_name %>
  password:
